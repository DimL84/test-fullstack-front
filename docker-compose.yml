services:
  server:
    build:
      context: .
      dockerfile: ./.docker/server/Dockerfile
    ports:
      - "8001:8000"
    networks:
      - test_entretien
    volumes:
      - ./lib:/app/lib,
      - .:/app
    command: ["deno", "run", "--allow-net", "--allow-read", "/app/lib/main.ts"]

networks:
  test_entretien:
